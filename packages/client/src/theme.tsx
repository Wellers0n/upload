import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#3b4d00',
      contrastText: '#fff',
      
    },
    secondary: {
      main: '#363636',
      contrastText: '#fff',
    },
  },
});

export default theme